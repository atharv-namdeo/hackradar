# .env.example

# Base64 encoded JSON credentials for Google authentication
GOOGLE_CREDENTIALS_JSON="<base64_encoded_credentials>"

# Optional token path for Google authentication
GOOGLE_TOKEN_PATH="<optional_path>"